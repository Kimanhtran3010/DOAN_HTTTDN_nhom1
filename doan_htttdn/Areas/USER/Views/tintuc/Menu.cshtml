@model List<doan_htttdn.FF.Menu_Article>
@{
    Layout = null;
}

@{ 
    int i = 0;
<div class="header">
    <div class="container">
        <div class="head-top">
            <div class=" h_menu4" style="float:left">
                <ul class="memenu skyblue">
                    <li class="active grid"><a class="color8" href="index.html">Home</a></li>
                    @foreach (var item in Model)
                    {
                        
                        if (i < 3)
                        {
                            <li>@Html.ActionLink(item.Name_Menu, "InNewscate", new { @cateid = item.ID_Menu })</li>
                        }
                        else
                        {
                            break;
                        }
                        i++;
                    }

                    @*<li class="grid">
            <a class="color2" href="#">	Lookbook</a>

        </li>*@

                </ul>
            </div>

            <div class="clearfix"> </div>
        </div>
    </div>

</div>
    }